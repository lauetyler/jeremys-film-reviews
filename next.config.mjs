/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/jeremys-film-reviews",
    images: {
        unoptimized: true,
    }
};

export default nextConfig;
